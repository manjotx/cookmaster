import { Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function LikesScreen() {
  return (
    <SafeAreaView>
    <View>
      <Text className='text-blue-500'>likes</Text>
    </View>
    </SafeAreaView>
  );
}
